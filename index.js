// Code your solution in this file.
let studentsNames = ['Sophie', 'Abby', 'Mia', 'Scarlett', 'Sophia']; 

function other(students) {
  return students.toLowerCase()
}

function lowerCaseStudentNames(names) {
  return names.map(other)
}

const fullNames: 

function nameToAttributes(organize) {
  return organize.map(function (name))
  const firstName
  //split name here
  const lastName //split name here
  return {firstName: }
}